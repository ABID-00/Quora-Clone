<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Posts</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <h1>Quora Posts</h1>

    <% for(post of posts) { %>
        <div class="posts">
            <h2>@<%=post.username%></h2>
            <p><%=post.content %></p>
            <a class="toggle" href="http://localhost:4090/posts/<%= post.id %>">See post</a>
            <a class="toggle" href="http://localhost:4090/posts/<%= post.id %>/edit">Edits</a>
            <form action="/posts/<%=post.id %>?_method=DELETE" method="post">
                <button class="toggle">Delete Post</button>
            </form>
        </div>
    <% } %>
    <br><br>
    <a href="http://localhost:4090/posts/new" id="new">Create New Post!</a>
    
</body>
</html>